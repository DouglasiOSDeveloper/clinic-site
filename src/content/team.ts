export type TeamMember = {
  nome: string;
  cargo: string;
  bioCurta: string;
  registro?: string;
  foto: string;
  instagram?: string;
  portfolioUrl?: string;
};

export const team: TeamMember[] = [
  // preencha com seus dados
];
